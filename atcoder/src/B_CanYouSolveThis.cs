// https://atcoder.jp/contests/abc121/tasks/abc121_b

internal class B_CanYouSolveThis
{
    public static void Solution()
    {
        // int[] nmc = Console.ReadLine()
        //            .Split()
        //            .Select(int.Parse)
        //            .ToArray();
        // int[] bnm = Console.ReadLine()
        //                    .Split()
        //                    .Select(int.Parse)
        //                    .ToArray();
        // int result = 0;
        // for (int n = 0; n < nmc[0]; n++)
        // {
        //     int[] anm = Console.ReadLine()
        //                        .Split()
        //                        .Select(int.Parse)
        //                        .ToArray();
        //     int sum = nmc[2];
        //     for (int m = 0; m < nmc[1]; m++)
        //     {
        //         sum += bnm[m] * anm[m];
        //     }
        //     if (sum > 0) { result++; }

        // }
        // Console.WriteLine(result);
    }
}